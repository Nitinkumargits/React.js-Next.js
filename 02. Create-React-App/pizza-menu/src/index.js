import React from "react";
import ReactDOM from "react-dom/client";

function App(params) {
  return <h1>Helllow form nitin</h1>;
}

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
